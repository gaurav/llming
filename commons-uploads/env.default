# Create an API key/secret pair at flickr.com/services/apps/create,
# then copy this file to .env (gitignored) and fill it in. reconcile.py loads .env itself.
# Only the key is needed for reading public albums; the secret is kept for future OAuth use.
FLICKR_API_KEY=
FLICKR_API_SECRET=
